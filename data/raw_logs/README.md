# Raw Logs

Use this folder for local raw telemetry or simulator logs that should not be committed to GitHub.

The `.gitignore` keeps files in this folder untracked except this README. Existing flight logs may also live in `data/logs/`, which is ignored for the same reason.
